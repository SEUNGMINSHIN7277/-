{
  "name": "dialogue-shorts-factory-renderer",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright HTML->PNG renderer for the call-UI frames and avatars.",
  "dependencies": {
    "playwright": "^1.56.1"
  },
  "scripts": {
    "postinstall": "playwright install chromium"
  }
}
