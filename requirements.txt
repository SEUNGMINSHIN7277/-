# --- 핵심 (dry-run 포함 모든 모드에서 필요) ---
imageio-ffmpeg>=0.4.9      # 정적 FFmpeg 바이너리 제공 (시스템 ffmpeg 있으면 그걸 사용)

# --- 무자본(무료) 부품 ---
edge-tts>=6.1             # 무료 TTS(키 불필요, 한국어 뉴럴, 단어 타임스탬프) — 권장 기본
# Gemini(무료 LLM)는 SDK 없이 urllib REST 호출이라 추가 패키지 불필요(GEMINI_API_KEY만).

# --- 유료/선택 (실모드에서 해당 기능 쓸 때만) ---
anthropic>=0.40            # 대본 생성 LLM (모듈 B) — 유료. 무료로 쓰려면 Gemini 사용
google-api-python-client>=2.100   # YouTube 업로드 (모듈 G)
google-auth>=2.23
google-auth-oauthlib>=1.1
# requests 는 사용하지 않음(urllib 표준 라이브러리로 구현). 필요 시 추가 가능.
# python-dotenv 불필요(자체 .env 파서 내장).

# Coupang / Pexels / ElevenLabs 는 표준 urllib 로 호출하므로 별도 패키지 불필요.
